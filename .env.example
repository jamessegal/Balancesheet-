# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/balancesheet

# Redis (for BullMQ job queue — used later)
REDIS_URL=redis://localhost:6379

# Auth (generate a real secret for production: openssl rand -base64 32)
AUTH_SECRET=dev-secret-change-in-production
AUTH_TRUST_HOST=true

# Xero OAuth
XERO_CLIENT_ID=your-xero-client-id
XERO_CLIENT_SECRET=your-xero-client-secret
# 32-byte encryption key for Xero tokens (generate with: openssl rand -hex 32)
XERO_TOKEN_ENCRYPTION_KEY=generate-with-openssl-rand-hex-32

# S3 / Cloudflare R2 (not needed for Step 1)
# S3_ENDPOINT=
# S3_ACCESS_KEY=
# S3_SECRET_KEY=
# S3_BUCKET=balancesheet-docs
